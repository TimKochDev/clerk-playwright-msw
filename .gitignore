# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

/docs
/scripts/*.lnk

keys.js
webpack-stats.json
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# For playwright tests, clerk needs a testing token to disable bot protection.
# Since clerk's playwright kit doesn't work in combination with msw,
# we need to fetch, store, and use the token ourselves.
.clerkTestingToken
